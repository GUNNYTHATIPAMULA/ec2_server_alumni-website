services:
  frontend:
    build: .
    container_name: react-frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_BASE_API_URL=http://localhost:8000
    networks:
      - alumni-network

networks:
  alumni-network:
    external: true
